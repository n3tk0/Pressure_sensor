name: Build Windows EXE

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'main.py'
      - 'sensor_core.py'
      - 'dpg_theme.py'
      - 'main.spec'
      - 'fonts/**'
      - 'icon.ico'
      - '.github/workflows/build-exe.yml'
  workflow_dispatch:   # allow manual trigger from GitHub Actions tab

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller dearpygui pyserial

      - name: Build EXE with PyInstaller
        run: |
          pyinstaller main.spec --clean --noconfirm

      - name: Verify EXE was created
        run: |
          if (Test-Path "dist\CisternAnalytics.exe") {
            $size = (Get-Item "dist\CisternAnalytics.exe").length / 1MB
            Write-Host "CisternAnalytics.exe built successfully ($([math]::Round($size,1)) MB)"
          } else {
            Write-Error "Build failed — CisternAnalytics.exe not found"
            exit 1
          }

      - name: Upload EXE as artifact
        uses: actions/upload-artifact@v4
        with:
          name: CisternAnalytics-windows
          path: dist/CisternAnalytics.exe
          retention-days: 30
